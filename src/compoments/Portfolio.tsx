import React from 'react';

const Portfolio: React.FC = () => (
    <section className="portfolio" id="portfolio">
        {/* ...contenu portfolio ici... */}
    </section>
);

export default Portfolio;
